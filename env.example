# Bybit API Configuration
BYBIT_API_KEY=your_api_key_here
BYBIT_API_SECRET=your_api_secret_here
BYBIT_DEMO_MODE=true  # Set to false for live trading

# Trading Configuration
SYMBOL=BTCUSDT
LEVERAGE=10  # Moderate leverage for $100 balance
INITIAL_CAPITAL=100  # USD - Your demo balance

# Grid Trading Parameters - Automatic calculation like backtest
GRID_RANGE_PERCENT=3.0  # 3% range around current price (like backtest)
GRID_LEVELS=5  # Fewer levels for smaller balance
GRID_ORDER_SIZE=0.001  # Minimum order size for BTCUSDT futures

# DCA Trading Parameters - Optimized for $100 balance
DCA_ENABLED=true
DCA_TRIGGER_PERCENT=2.0  # Less frequent triggers for smaller balance
DCA_ORDER_SIZE=0.001  # Minimum order size for BTCUSDT futures
DCA_MAX_ORDERS=2  # Fewer DCA levels for smaller balance

# Risk Management - Optimized for better returns
KILL_SWITCH_ENABLED=true
MAX_DRAWDOWN_PERCENT=15.0  # Tighter drawdown control
BREAKEVEN_ENABLED=true
PARTIAL_PROFIT_ENABLED=true
PARTIAL_PROFIT_PERCENT=30.0  # Take 30% profit at 1.5x entry for faster turnover

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/trading_bot.log
LOG_RETENTION_DAYS=30

# Database Configuration
DATABASE_URL=sqlite:///trading_bot.db

# Alert Configuration
ALERTS_ENABLED=true
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id

# Backtesting Configuration
BACKTEST_START_DATE=2024-01-01
BACKTEST_END_DATE=2024-12-31
BACKTEST_INITIAL_CAPITAL=100

# Trading Strategy Parameters (Used by both backtest and live trading)
GRID_WIN_RATE=80  # Percentage (80% = 8 out of 10 trades win)
GRID_PROFIT_PERCENT=0.7  # Fixed profit percentage for grid trades
GRID_LOSS_PERCENT=0.15  # Fixed loss percentage for grid trades
DCA_WIN_RATE=65  # Percentage (65% = 13 out of 20 DCA trades win)
DCA_PROFIT_PERCENT=1.0  # Fixed profit percentage for DCA trades
DCA_LOSS_PERCENT=0.2  # Fixed loss percentage for DCA trades
SLIPPAGE_PERCENT=0.01  # Fixed slippage percentage
